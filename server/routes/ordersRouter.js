const express = require('express');
const router = express.Router();

// Orders
router.get('/',);// Отримання списку замовлень
router.post('/',);// Створення нового замовлення
router.put('/:id',);// Оновлення замовлення за ідентифікатором
router.delete('/:id',);// Видалення замовлення за ідентифікатором

module.exports = router;
